// See https://code.visualstudio.com/docs/debugtest/debugging-configuration
// If breakpoints are not attaching, try launching the command "Debug: Diagnose Breakpoint Problems"
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/examples"
      ],
      "preLaunchTask": "Pre Run Extension",
      "sourceMaps": true
    }
  ]
}
