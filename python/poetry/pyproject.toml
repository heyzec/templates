[tool.poetry]
name = "nix-templates-python-poetry"
version = "0.1.0"
package-mode = false

[tool.poetry.dependencies]
python = "~3.12"
numpy = "^2.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

